import React from "react";
import { FilterDisplayProps } from "../../commons/manage/_types/props/filterDisplayProps";


export function MentorFilterDisplay({
    id,
    values,
    onChange,
    disabled,
}: FilterDisplayProps) {
    return (<React.Fragment></React.Fragment>)
}